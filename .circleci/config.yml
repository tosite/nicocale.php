# ~/.circleci/config.yml
version: 2
jobs:
  build:
    docker:
      - image: circleci/php:7.3.2-apache-node-browsers
    parallelism: 4
    steps:
      - run:
          command: |
            echo $(circleci tests glob "foo/**/*" "bar/**/*")
